---
apiVersion: v1
kind: Service
metadata:
  name: simple-api
  namespace: prod
  labels:
    app: simple-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: simple-api
